# 🔌 어댑터 패턴 (Adapter Pattern)

## 📋 목차

* 어댑터 패턴이란?
* 어댑터 패턴의 구조
* 실습 예제 1: 메시지 시스템
* 실습 예제 2: 디스플레이 어댑터
* 어댑터 패턴의 장단점
* 실무 활용 사례

---

## 🔌 어댑터 패턴이란?

### 핵심 개념

> **"호환되지 않는 인터페이스를 가진 객체들이 함께 작동할 수 있도록 중간에서 변환해주는 패턴"**

조금 더 풀어서 말하면,
어댑터 패턴은 **이미 존재하는 코드(Adaptee)** 를 수정하지 않고,
**클라이언트가 기대하는 인터페이스(Target)** 에 맞게 **중간에서 번역해주는 역할**을 한다.

즉,

* 클라이언트는 새로운 인터페이스만 알면 되고
* 기존 시스템은 자기 방식 그대로 유지되며
* 변환 책임은 오직 어댑터가 담당한다

👉 **변경에 취약한 코드를 보호하기 위한 패턴**이라고 볼 수 있다.

---

### 실생활 비유

* 🇰🇷 한국 플러그 → 🇪🇺 유럽 콘센트 (변환을 위해서 어댑터가 필요하다!)
* USB-C → HDMI 변환기
* 220V → 110V 변압기

이 비유들의 공통점은

> **“기존 기기와 환경을 바꾸지 않고, 중간 장치만 추가한다”**는 것이다.

어댑터 패턴도 동일하게
**기존 클래스와 클라이언트 사이에 ‘중간 장치’를 둔다**

---

## 어댑터 패턴의 필요성

### 문제 상황

실무에서는 다음과 같은 상황이 자주 발생한다.

* 이미 운영 중인 **레거시 시스템**과 신규 시스템이 함께 동작해야 할 때
* 외부 라이브러리(API, SDK)의 인터페이스가 우리 코드와 맞지 않을 때
* 여러 곳에서 사용 중인 코드를 **함부로 수정할 수 없을 때**

이때 기존 코드를 직접 수정하면

* 사이드 이펙트 발생 가능성 증가
* 테스트 비용 증가
* 배포 리스크 증가

---

### 해결 방법

**어댑터를 중간에 두어 인터페이스를 변환한다**

> “기존 코드는 그대로 두고,
> 새로운 요구사항은 새로운 클래스(어댑터)로 해결한다.”

이 방식은 **개방-폐쇄 원칙(OCP)** 을 자연스럽게 만족시킨다.

---

## 어댑터 패턴의 구조

```
Client → Target Interface ← Adapter → Adaptee
```

### 4가지 핵심 요소

* **Target (목표 인터페이스)**
  클라이언트가 기대하고 사용하는 인터페이스
  → *“나는 이 메서드만 알면 돼”*

* **Adaptee (적응 대상)**
  이미 존재하지만 인터페이스가 맞지 않는 클래스
  → 보통 레거시 코드, 외부 라이브러리

* **Adapter (어댑터)**
  Target을 구현하면서 내부에서 Adaptee를 호출
  → 변환 로직이 집중되는 곳

* **Client (클라이언트)**
  Target 인터페이스만 의존
  → Adaptee의 존재를 모른다

**Adapter는 유일하게 양쪽을 모두 아는 클래스**다.

---

## 예제 1: 메시지 시스템

### 시나리오

* 기존 시스템: `OldMessageSystem`

  * 배열 기반 데이터 전달
  * 결과 코드 반환
* 신규 요구사항:

  * 메시지와 수신자를 명확한 파라미터로 전달
* 목표:

  * 기존 시스템 수정 X
  * 새로운 인터페이스로 사용 O

 **실무에서 가장 흔한 레거시 연동 시나리오**

---

### 예제 1 - Target Interface

```java
interface ModernMessageSender {
    void sendMessage(String message, String recipient);
}
```

#### 보충 설명

* 클라이언트 입장에서 가장 이상적인 형태
* 의미가 명확하고, 실수할 여지가 적음
* “앞으로 우리가 쓰고 싶은 표준 인터페이스”

---

### 예제 1 - Adaptee

```java
interface OldMessageSender {
    int send(String[] messageData);
}
```

#### 보충 설명

* 과거 시스템에서 흔히 볼 수 있는 방식
* 배열 인덱스에 의미가 숨어 있음
* 성공/실패를 숫자로 표현 → 해석 책임이 호출자에게 있음

**바꾸기 어렵지만, 없앨 수도 없는 코드**

---

### 🔧 예제 1 - Adapter

```java
class MessageAdapter implements ModernMessageSender {
    private OldMessageSender oldSystem;

    public MessageAdapter(OldMessageSender oldSystem) {
        this.oldSystem = oldSystem;
    }

    @Override
    public void sendMessage(String message, String recipient) {
        String[] messageData = {message, recipient};
        int result = oldSystem.send(messageData);

        if (result != 1) {
            System.out.println("Failed to send message");
        }
    }
}
```

#### 핵심 포인트

* 새로운 인터페이스를 구현하면서
* 내부에서는 레거시 시스템 호출
* 결과 코드 해석 책임도 어댑터가 가짐

실무에서는 여기서

* 예외 변환
* 로깅
* 모니터링
  을 함께 처리하는 경우가 많다.

---

### 예제 1 - Client 사용

```java
ModernMessageSender adapter = new MessageAdapter(oldSystem);
adapter.sendMessage("Hello", "john@example.com");
```

#### 결과

* 클라이언트는 레거시 시스템을 전혀 모름
* 인터페이스 변경 시 영향 최소화

---

## 예제 2: 디스플레이 어댑터

### 시나리오

* USB / HDMI / VGA
* 메서드명, 파라미터, 설정 방식 전부 다름
* 목표:

  * 클라이언트는 “디스플레이 한다”만 신경 쓰게

 **인터페이스 통일 + 각 구현의 설정은 유지**

---

### 설계 의도 보충

* 단순히 메서드 이름을 맞추는 것이 목적 X
* **각 방식의 차이를 어댑터가 책임지게 만드는 것**이 핵심

---

## 실행 결과

```
Displaying via USB with data: Video Data
Displaying via HDMI with resolution: 1080p
Displaying via VGA with high quality: true
```

서로 다른 구현을
**하나의 리스트, 하나의 인터페이스로 처리 가능**

---

## 어댑터 패턴의 장점 (실무 관점)

* 기존 코드 수정 없이 확장 가능 (OCP)
* 변환 책임이 명확히 분리됨 (SRP)
* 테스트 및 교체 용이
* 외부 의존성 차단 (Anti-Corruption Layer)

---

## 어댑터 패턴의 단점

* 클래스 수 증가
* 구조 파악 비용 증가
* 어댑터 남용 시 오히려 복잡해짐

 **경계 지점에서만 사용하는 것이 중요**

---

## 실무 활용 사례 (백엔드 기준)

### 1. 외부 API 통합

* 결제, 로그인, 지도 API
* SDK마다 인터페이스 상이
* → 내부 인터페이스 + 어댑터로 통일

### 2. 레거시 시스템 마이그레이션

* 구 DB / 신 DB 병행
* 점진적 전환 가능

### 3. MSA 환경

* 서비스 간 DTO 구조 차이
* Adapter에서 내부 모델로 변환

---

## 핵심 정리

> **“어댑터 패턴은 인터페이스가 아니라
> 시스템의 경계를 다루는 패턴이다.”**

### 언제 써야 할까?

* 레거시 코드를 보호하고 싶을 때
* 외부 의존성을 내부로 전파하고 싶지 않을 때
* 점진적인 리팩토링이 필요할 때